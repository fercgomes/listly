import { useParams } from "react-router-dom";
import { CreateListForm } from "../components/NewListDialog";
import { useFirestore } from "reactfire";

export const createList = async (list: CreateListForm) => {
  return "aaaaaaa";
};
